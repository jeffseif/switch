[tox]
envlist = py35
 
[testenv]
deps =
    coverage
    flake8
    mock
    pytest
commands =
    coverage run --source=switch/,tests/ -m pytest --strict {posargs:tests}
    flake8 .
sitepackages=True
 
[flake8]
ignore = E125
max-line-length = 120
max-complexity = 10
exclude = .git,.tox,venv
